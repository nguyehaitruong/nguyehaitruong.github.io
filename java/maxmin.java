package org.example;

public class maxmin {
    public static void main(String[] args) {

        int a=1,b=2,c=3,d=4;
        int max1,max2,max;
        max1=Math.max(a,b);
        max2=Math.max(c,d);
        max=Math.max(max1,max2);
        System.out.println("giá trị lơn nhất la "+max);
    }
}
